using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class AdvertSt : MonoBehaviour
{
   // public RewardedAds rewardads1;
    // Start is called before the first frame update
    void Start()
    {
     //   RewardedAds.LoadAd();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
